@model Expending_Card.Models.ExpendingViewModel

@{
    ViewBag.Title = "記帳明細";
    Layout = "_Layout";
}

<h2>記帳明細</h2>
<ol class="mt-3">
@foreach (var detail in @Model.DetailViewModel.Details)
{
    <li>@detail.Date: @detail.Detail - @detail.Price  (@detail.Card.Name)</li>
}
</ol>
